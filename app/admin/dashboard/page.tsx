"use client";

import Link from "next/link";
import { Activity, Building2, FileText, School, Users } from "lucide-react";
import { useEffect, useState } from "react";
import { adminApi } from "@/lib/api/admin";
import { apiErrorMessage } from "@/lib/api/client";
import type { ApiAdminOverviewDto } from "@/lib/api/dtos";
import { PageContainer, PageHeader } from "@/components/layout/page-header";
import { PageTransition } from "@/components/shared/page-transition";
import { Avatar } from "@/components/shared/avatar";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { StatCard } from "@/components/dashboard/stat-card";
import { toPersianNumber } from "@/lib/utils";

export default function AdminDashboardPage() {
  const [overview, setOverview] = useState<ApiAdminOverviewDto | null>(null); const [error, setError] = useState<string | null>(null);
  const load = async () => { try { setError(null); setOverview(await adminApi.overview()); } catch (reason) { setError(apiErrorMessage(reason, "دریافت وضعیت سامانه انجام نشد.")); } };
  useEffect(() => { void load(); }, []); // eslint-disable-line react-hooks/exhaustive-deps
  if (error) return <PageTransition><PageContainer><PageHeader eyebrow="مرکز مدیریت" title="نمای کلی سامانه" description="وضعیت مدارس، کاربران و آزمون‌ها." breadcrumbs={[{ label: "مدیر سامانه" }]}/><Card><CardContent className="p-8 text-center"><p className="text-destructive">{error}</p><Button className="mt-5" onClick={() => void load()}>تلاش دوباره</Button></CardContent></Card></PageContainer></PageTransition>;
  const data = overview;
  return <PageTransition><PageContainer><PageHeader eyebrow="مرکز مدیریت" title="نمای کلی سامانه" description="وضعیت واقعی مدارس، کاربران و آزمون‌های شبکه را در یک نمای عملیاتی دنبال کنید." breadcrumbs={[{ label: "مدیر سامانه" }, { label: "نمای کلی" }]}/>{!data ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Array.from({ length: 4 }).map((_, index) => <Card className="h-32 animate-soft-pulse bg-muted" key={index}/>)}</div> : <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={School} label="مدارس فعال" value={toPersianNumber(data.school_count)} detail="فضاهای سازمانی فعال" tone="indigo"/><StatCard icon={Users} label="کل کاربران" value={toPersianNumber(data.user_count)} detail={`${toPersianNumber(data.user_counts.student || 0)} دانش‌آموز و ${toPersianNumber(data.user_counts.teacher || 0)} آموزگار`} tone="teal"/><StatCard icon={FileText} label="کل آزمون‌ها" value={toPersianNumber(data.exam_count)} detail={`${toPersianNumber(data.active_exam_count)} آزمون اکنون فعال است`} tone="violet"/><StatCard icon={Activity} label="حساب بدون مدرسه" value={toPersianNumber(data.unassigned_user_count)} detail="نیازمند تعیین مدرسه" tone="amber"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>کاربران تازه</CardTitle><CardDescription>آخرین حساب‌های ایجادشده در سامانه</CardDescription></div><Button asChild variant="ghost" size="sm"><Link href="/admin/users">همه کاربران</Link></Button></CardHeader><CardContent><div className="divide-y">{data.recent_users.map((user) => <div className="flex items-center gap-3 py-3 first:pt-0" key={user.id}><Avatar name={user.full_name} size="sm"/><div className="min-w-0 flex-1"><p className="text-sm font-bold">{user.full_name}</p><p className="mt-1 truncate text-[11px] text-muted-foreground">{user.email} · {user.school?.name || "بدون مدرسه"}</p></div><Badge variant={user.role === "admin" ? "default" : user.role === "teacher" ? "teal" : "neutral"}>{user.role === "admin" ? "مدیر" : user.role === "teacher" ? "آموزگار" : "دانش‌آموز"}</Badge></div>)}</div></CardContent></Card><Card><CardHeader><CardTitle>میانبرهای مدیریت</CardTitle><CardDescription>مدیریت موجودیت‌های عملیاتی</CardDescription></CardHeader><CardContent className="grid gap-2"><Button asChild variant="secondary"><Link href="/admin/users">مدیریت کاربران <Users className="h-4 w-4"/></Link></Button><Button asChild variant="secondary"><Link href="/admin/schools">مدیریت مدارس <Building2 className="h-4 w-4"/></Link></Button><Button asChild variant="secondary"><Link href="/admin/exams">نظارت بر آزمون‌ها <FileText className="h-4 w-4"/></Link></Button></CardContent></Card></div><Card className="mt-6"><CardHeader><CardTitle>آزمون‌های به‌روز شده</CardTitle><CardDescription>آخرین تغییرات ثبت‌شده در شبکه</CardDescription></CardHeader><CardContent>{data.recent_exams.length ? <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{data.recent_exams.map((exam) => <div className="rounded-2xl border p-4" key={exam.id}><div className="flex items-center justify-between gap-2"><Badge variant={exam.status === "active" ? "success" : exam.status === "draft" ? "warning" : "neutral"}>{exam.status === "active" ? "فعال" : exam.status === "draft" ? "پیش‌نویس" : exam.status === "scheduled" ? "زمان‌بندی" : "پایان‌یافته"}</Badge><span className="text-[11px] text-muted-foreground">{exam.school?.name || "بدون مدرسه"}</span></div><p className="mt-3 truncate text-sm font-black">{exam.title}</p><p className="mt-1 text-xs text-muted-foreground">{exam.teacher_name} · {toPersianNumber(exam.participant_count)} شرکت‌کننده</p></div>)}</div> : <p className="text-sm text-muted-foreground">هنوز آزمونی ثبت نشده است.</p>}</CardContent></Card></>}</PageContainer></PageTransition>;
}
