import { StudentRouteShell } from "@/components/layout/student-route-shell";
export default function StudentLayout({ children }: { children: React.ReactNode }) { return <StudentRouteShell>{children}</StudentRouteShell>; }
