"use client";

import Link from "next/link";
import { Archive, CalendarDays, CheckCircle2, Clock3, Copy, Edit3, Eye, FileText, ListChecks, RotateCcw, Settings2, UsersRound } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import type { Exam } from "@/lib/types/domain";
import { QuestionRenderer } from "@/components/exam/question-renderer";
import { EmptyState } from "@/components/shared/empty-state";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog } from "@/components/ui/dialog";
import { Skeleton } from "@/components/ui/skeleton";
import { useTeacherExams } from "@/hooks/use-teacher-exams";
import { useToastStore } from "@/lib/state/toast-store";
import { cn, formatDateTime, toPersianNumber } from "@/lib/utils";

const statusMap = { active: ["در حال برگزاری", "success"], scheduled: ["زمان‌بندی شده", "default"], draft: ["پیش‌نویس", "warning"], completed: ["پایان یافته", "neutral"], archived: ["بایگانی", "neutral"] } as const;
export function TeacherExamDetailWorkspace({ examId }: { examId: string }) {
  const { exams, loading, detailLoadingId, initialized, duplicateExam, completeExam, archiveExam, restoreExam, loadExam } = useTeacherExams();
  const toast = useToastStore((state) => state.push);
  const [previewOpen, setPreviewOpen] = useState(false); const [completeOpen, setCompleteOpen] = useState(false); const [archiveOpen, setArchiveOpen] = useState(false);
  useEffect(() => { void loadExam(examId); }, [examId, loadExam]);
  const exam = useMemo(() => exams.find((item) => item.id === examId), [exams, examId]);
  if ((loading && !initialized) || !initialized || detailLoadingId === examId) return <DetailSkeleton/>;
  if (!exam) return <EmptyState title="آزمون پیدا نشد" description="ممکن است این آزمون در فضای دیگری ایجاد شده باشد یا دیگر در دسترس نباشد." action={{ label: "بازگشت به آزمون‌ها", onClick: () => window.location.assign("/teacher/exams") }}/>;
  const currentExam = exam;
  const [label, variant] = statusMap[currentExam.status];
  async function duplicate() { const copy = await duplicateExam(currentExam.id); if (copy) toast({ title: "کپی آزمون ساخته شد", description: "نسخهٔ جدید به‌صورت پیش‌نویس آمادهٔ ویرایش است.", variant: "success" }); }
  async function complete() { await completeExam(currentExam.id); setCompleteOpen(false); toast({ title: "آزمون پایان یافت", description: "دانش‌آموزان دیگر نمی‌توانند این آزمون را شروع کنند.", variant: "success" }); }
  async function archive() { await archiveExam(currentExam.id); setArchiveOpen(false); toast({ title: "آزمون بایگانی شد", description: "برای بازیابی، فیلتر بایگانی را در صفحهٔ آزمون‌ها باز کنید." }); }
  async function restore() { await restoreExam(currentExam.id); toast({ title: "آزمون بازیابی شد", variant: "success" }); }
  return <><div className="grid gap-6 xl:grid-cols-[1.24fr_.76fr]"><section className="space-y-6"><Card><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><div><CardTitle>نمای کلی آزمون</CardTitle><CardDescription>{exam.subject} · {exam.grade} · {exam.className}</CardDescription></div><Badge variant={variant}>{label}</Badge></div></CardHeader><CardContent><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[{ icon: CalendarDays, label: "شروع", value: formatDateTime(exam.schedule.startAt) }, { icon: Clock3, label: "مدت", value: `${toPersianNumber(exam.settings.durationMinutes)} دقیقه` }, { icon: ListChecks, label: "سؤال‌ها", value: `${toPersianNumber(exam.questionCount)} سؤال` }, { icon: UsersRound, label: "مشارکت", value: `${toPersianNumber(exam.participantCount)} دانش‌آموز` }].map((item) => { const Icon = item.icon; return <div key={item.label} className="rounded-xl bg-muted/55 p-3"><Icon className="h-4 w-4 text-primary"/><p className="mt-4 text-[10px] font-bold text-muted-foreground">{item.label}</p><p className="mt-1 text-xs font-extrabold">{item.value}</p></div>; })}</div><div className="mt-6 rounded-2xl border bg-surface p-4"><p className="text-xs font-bold text-muted-foreground">توضیح آزمون</p><p className="mt-2 text-sm leading-7">{exam.description}</p>{exam.instructions && <p className="mt-4 border-t pt-4 text-xs leading-6 text-muted-foreground"><b className="text-foreground">راهنمای دانش‌آموز: </b>{exam.instructions}</p>}</div></CardContent></Card><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>سؤال‌های آزمون</CardTitle><CardDescription>{toPersianNumber(exam.questions.length)} سؤال با مجموع {toPersianNumber(exam.settings.totalMarks)} نمره</CardDescription></div><Button asChild size="sm" variant="outline"><Link href={`/teacher/exams/${exam.id}/edit`}>ویرایش سؤال‌ها</Link></Button></CardHeader><CardContent><div className="divide-y rounded-xl border px-4">{exam.questions.map((question) => <div className="flex items-center gap-3 py-3" key={question.id}><span className="grid h-7 w-7 place-items-center rounded-lg bg-primary/10 text-xs font-black text-primary">{toPersianNumber(question.order)}</span><p className="min-w-0 flex-1 truncate text-sm font-bold">{question.stem || "بدون متن سؤال"}</p><span className="text-[11px] font-bold text-muted-foreground">{question.type === "single_choice" ? "چندگزینه‌ای" : question.type === "multiple_choice" ? "چندپاسخی" : question.type === "true_false" ? "درست/نادرست" : question.type === "short_answer" ? "کوتاه" : "تشریحی"}</span><span className="text-[11px] text-muted-foreground">{toPersianNumber(question.points)} نمره</span></div>)}</div></CardContent></Card></section><aside className="space-y-5"><Card><CardHeader><CardTitle>اقدام‌ها</CardTitle></CardHeader><CardContent className="grid gap-2"><Button asChild><Link href={`/teacher/exams/${exam.id}/edit`}><Edit3 className="h-4 w-4"/>ویرایش آزمون</Link></Button><Button variant="secondary" onClick={() => setPreviewOpen(true)}><Eye className="h-4 w-4"/>پیش‌نمایش سؤال‌ها</Button><Button variant="secondary" onClick={() => void duplicate()}><Copy className="h-4 w-4"/>ساخت یک کپی</Button>{exam.status === "active" && <Button variant="outline" onClick={() => setCompleteOpen(true)}><CheckCircle2 className="h-4 w-4"/>اتمام آزمون</Button>}{exam.status === "archived" ? <Button variant="outline" onClick={() => void restore()}><RotateCcw className="h-4 w-4"/>بازیابی آزمون</Button> : <Button variant="outline" className="text-destructive hover:text-destructive" onClick={() => setArchiveOpen(true)}><Archive className="h-4 w-4"/>بایگانی آزمون</Button>}{["active", "completed"].includes(exam.status) && <Button asChild variant="outline"><Link href="/teacher/results"><UsersRound className="h-4 w-4"/>مشاهده نتایج</Link></Button>}</CardContent></Card><Card><CardHeader><CardTitle>تنظیمات انتشار</CardTitle></CardHeader><CardContent className="space-y-3 text-sm">{[{ label: "بازگشت به سؤال‌ها", value: exam.settings.allowBackNavigation ? "مجاز" : "غیرفعال" }, { label: "ترتیب سؤال‌ها", value: exam.settings.randomizeQuestions ? "تصادفی" : "ثابت" }, { label: "نمایش نتیجه", value: exam.settings.resultVisibility === "immediate" ? "فوری" : exam.settings.resultVisibility === "pending" ? "پس از بررسی" : "انتشار دستی" }, { label: "تعداد تلاش", value: `${toPersianNumber(exam.settings.attemptLimit)} بار` }, { label: "کلید پاسخ", value: "برای دانش‌آموز نمایش داده نمی‌شود" }].map((item) => <div key={item.label} className="flex items-center justify-between gap-3 border-b pb-3 last:border-0 last:pb-0"><span className="font-bold text-muted-foreground">{item.label}</span><span className="text-xs font-extrabold">{item.value}</span></div>)}</CardContent></Card><Card><CardHeader><CardTitle>زمان‌بندی</CardTitle></CardHeader><CardContent><p className="text-xs font-bold text-muted-foreground">شروع</p><p className="mt-1 text-sm font-extrabold">{formatDateTime(exam.schedule.startAt)}</p><p className="mt-4 text-xs font-bold text-muted-foreground">پایان دسترسی</p><p className="mt-1 text-sm font-extrabold">{formatDateTime(exam.schedule.endAt)}</p><p className="mt-3 rounded-lg bg-muted/60 p-2 text-[10px] text-muted-foreground">{exam.schedule.timezone}</p></CardContent></Card></aside></div><Dialog open={previewOpen} onClose={() => setPreviewOpen(false)} title="پیش‌نمایش آزمون" description="نمونه‌ای از نخستین سؤال برای دانش‌آموز." size="md">{exam.questions[0] ? <div className="rounded-2xl border bg-surface p-5"><p className="mb-4 text-sm font-black">{exam.questions[0].stem}</p><QuestionRenderer question={exam.questions[0]} value={null} onChange={() => undefined} disabled/></div> : <p className="text-sm text-muted-foreground">هنوز سؤالی برای نمایش وجود ندارد.</p>}<div className="mt-5 flex justify-end"><Button onClick={() => setPreviewOpen(false)}>بستن پیش‌نمایش</Button></div></Dialog><Dialog open={completeOpen} onClose={() => setCompleteOpen(false)} title="پایان آزمون؟" description="دسترسی دانش‌آموزان برای شروع آزمون بسته می‌شود. این اقدام قابل بازگشت نیست." size="sm"><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><Button variant="outline" onClick={() => setCompleteOpen(false)}>انصراف</Button><Button data-autofocus onClick={() => void complete()}><CheckCircle2 className="h-4 w-4"/>تأیید پایان آزمون</Button></div></Dialog><Dialog open={archiveOpen} onClose={() => setArchiveOpen(false)} title="بایگانی آزمون؟" description="این آزمون از فهرست‌های فعال پنهان می‌شود، اما سؤال‌ها و نتایج آن حفظ خواهند شد." size="sm"><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><Button variant="outline" onClick={() => setArchiveOpen(false)}>انصراف</Button><Button variant="destructive" data-autofocus onClick={() => void archive()}><Archive className="h-4 w-4"/>تأیید بایگانی</Button></div></Dialog></>;
}
function DetailSkeleton() { return <div className="grid gap-6 xl:grid-cols-[1.24fr_.76fr]"><Card className="p-6"><Skeleton className="h-7 w-1/3"/><Skeleton className="mt-8 h-32"/><Skeleton className="mt-6 h-60"/></Card><Card className="p-6"><Skeleton className="h-7 w-1/2"/><Skeleton className="mt-6 h-44"/><Skeleton className="mt-6 h-44"/></Card></div>; }
