"use client";

import { Check, ChevronDown, ChevronUp, CircleCheck, Copy, Eye, GripVertical, ListPlus, Plus, Save, Trash2 } from "lucide-react";
import { useMemo, useState } from "react";
import type { Question, QuestionOption, QuestionType } from "@/lib/types/domain";
import { QuestionRenderer } from "@/components/exam/question-renderer";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
import { cn, toPersianNumber } from "@/lib/utils";

const questionTypes: Array<{ type: QuestionType; label: string; description: string }> = [
  { type: "single_choice", label: "چندگزینه‌ای", description: "یک پاسخ درست" },
  { type: "multiple_choice", label: "چندپاسخی", description: "چند پاسخ درست" },
  { type: "true_false", label: "درست / نادرست", description: "یک انتخاب دوگانه" },
  { type: "short_answer", label: "پاسخ کوتاه", description: "پاسخ متنی کوتاه" },
  { type: "essay", label: "تشریحی", description: "پاسخ نوشتاری" },
];
const questionTypeLabel = Object.fromEntries(questionTypes.map((item) => [item.type, item.label])) as Record<QuestionType, string>;

function cloneQuestion(question: Question, id = question.id): Question {
  if (question.type === "single_choice") return { ...question, id, options: question.options.map((option, index) => ({ ...option, id: `${id}-option-${index}`, value: `${id}-option-${index}` })), correctOptionId: question.correctOptionId ? `${id}-option-${question.options.findIndex((option) => option.id === question.correctOptionId)}` : "" };
  if (question.type === "multiple_choice") return { ...question, id, options: question.options.map((option, index) => ({ ...option, id: `${id}-option-${index}`, value: `${id}-option-${index}` })), correctOptionIds: (question.correctOptionIds ?? []).map((correctId) => `${id}-option-${question.options.findIndex((option) => option.id === correctId)}`) };
  return { ...question, id };
}

export function blankQuestion(type: QuestionType, order: number): Question {
  const base = { id: `question-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 5)}`, order, stem: "", points: 1, required: true };
  if (type === "single_choice") return { ...base, type, options: createOptions(), correctOptionId: "" };
  if (type === "multiple_choice") return { ...base, type, options: createOptions(), correctOptionIds: [] };
  if (type === "true_false") return { ...base, type, correctAnswer: true };
  if (type === "short_answer") return { ...base, type, expectedAnswer: "", placeholder: "پاسخ کوتاه خود را بنویسید...", maxLength: 150 };
  return { ...base, type, placeholder: "پاسخ تشریحی خود را بنویسید...", maxLength: 1200, gradingNote: "" };
}
function createOptions(): QuestionOption[] { return [{ id: "option-1", label: "", value: "option-1" }, { id: "option-2", label: "", value: "option-2" }]; }
function questionIsComplete(question: Question) {
  if (!question.stem.trim()) return false;
  if (question.type === "single_choice") return question.options.length >= 2 && question.options.every((option) => option.label.trim()) && Boolean(question.correctOptionId);
  if (question.type === "multiple_choice") return question.options.length >= 2 && question.options.every((option) => option.label.trim()) && Boolean(question.correctOptionIds?.length);
  return true;
}

export function TeacherQuestionBuilder({ questions, onChange, className }: { questions: Question[]; onChange: (questions: Question[]) => void; className?: string }) {
  const [selectedId, setSelectedId] = useState(questions[0]?.id ?? "");
  const [adding, setAdding] = useState(false);
  const [previewOpen, setPreviewOpen] = useState(false);
  const [deleteOpen, setDeleteOpen] = useState(false);
  const selected = useMemo(() => questions.find((question) => question.id === selectedId) ?? questions[0], [questions, selectedId]);
  const ordered = questions.map((question, index) => ({ ...question, order: index + 1 }));
  const replace = (next: Question[]) => onChange(next.map((question, index) => ({ ...question, order: index + 1 })));
  function updateSelected(next: Question) { if (!selected) return; replace(questions.map((question) => question.id === selected.id ? next : question)); }
  function addQuestion(type: QuestionType) { const next = blankQuestion(type, questions.length + 1); replace([...questions, next]); setSelectedId(next.id); setAdding(false); }
  function duplicateSelected() { if (!selected) return; const copy = cloneQuestion(selected, `question-${Date.now().toString(36)}`); const titledCopy = { ...copy, stem: selected.stem ? `${selected.stem} (کپی)` : "" } as Question; replace([...questions, titledCopy]); setSelectedId(titledCopy.id); }
  function deleteSelected() { if (!selected) return; const remaining = questions.filter((question) => question.id !== selected.id); replace(remaining); setSelectedId(remaining[0]?.id ?? ""); setDeleteOpen(false); }
  function moveSelected(delta: -1 | 1) { if (!selected) return; const index = questions.findIndex((question) => question.id === selected.id); const target = index + delta; if (target < 0 || target >= questions.length) return; const next = [...questions]; [next[index], next[target]] = [next[target], next[index]]; replace(next); }
  return <div className={cn("grid gap-5 xl:grid-cols-[300px_minmax(0,1fr)]", className)}><Card className="overflow-hidden xl:max-h-[720px]"><CardHeader className="flex-row items-center justify-between gap-3"><div><CardTitle>سؤال‌های آزمون</CardTitle><CardDescription>{toPersianNumber(questions.length)} سؤال · {toPersianNumber(questions.reduce((sum, question) => sum + question.points, 0))} نمره</CardDescription></div><Button type="button" size="icon-sm" onClick={() => setAdding(!adding)} aria-label="افزودن سؤال"><Plus className="h-4 w-4"/></Button></CardHeader><CardContent className="relative"><div className="space-y-2 overflow-y-auto xl:max-h-[560px]">{adding && <QuestionTypePicker onPick={addQuestion}/>} {questions.length === 0 ? <div className="rounded-2xl border border-dashed bg-muted/30 p-6 text-center"><ListPlus className="mx-auto h-6 w-6 text-primary"/><p className="mt-3 text-sm font-black">اولین سؤال را اضافه کنید</p><p className="mt-1 text-xs leading-5 text-muted-foreground">از دکمهٔ بالا نوع سؤال را انتخاب کنید.</p></div> : ordered.map((question) => <button type="button" onClick={() => setSelectedId(question.id)} key={question.id} className={cn("flex w-full items-center gap-2.5 rounded-xl border p-3 text-right transition-all", selected?.id === question.id ? "border-primary bg-primary/[.06] shadow-sm" : "border-transparent bg-muted/55 hover:border-border hover:bg-card")}><GripVertical className="h-4 w-4 shrink-0 text-muted-foreground"/><span className={cn("grid h-7 w-7 shrink-0 place-items-center rounded-lg text-xs font-black", selected?.id === question.id ? "bg-primary text-primary-foreground" : "bg-card text-muted-foreground")}>{toPersianNumber(question.order)}</span><span className="min-w-0 flex-1"><span className="block truncate text-xs font-extrabold">{question.stem || "بدون متن سؤال"}</span><span className="mt-1 flex items-center gap-1.5 text-[10px] text-muted-foreground">{questionTypeLabel[question.type]} · {toPersianNumber(question.points)} نمره</span></span><span className={cn("h-2 w-2 rounded-full", questionIsComplete(question) ? "bg-emerald-500" : "bg-amber-500")} title={questionIsComplete(question) ? "آماده" : "نیازمند تکمیل"}/></button>)}</div></CardContent></Card>{selected ? <QuestionEditor question={selected} onChange={updateSelected} onPreview={() => setPreviewOpen(true)} onDuplicate={duplicateSelected} onDelete={() => setDeleteOpen(true)} onMove={moveSelected}/> : <QuestionEmpty onAdd={() => setAdding(true)}/>}<Dialog open={previewOpen} onClose={() => setPreviewOpen(false)} title="پیش‌نمایش دانش‌آموز" description="این بخش نحوهٔ نمایش سؤال در آزمون را نشان می‌دهد." size="md"><div className="rounded-2xl border bg-surface p-4"><QuestionRenderer question={selected ?? blankQuestion("single_choice", 1)} value={null} onChange={() => undefined} disabled/></div><div className="mt-5 flex justify-end"><Button onClick={() => setPreviewOpen(false)}>بستن پیش‌نمایش</Button></div></Dialog><Dialog open={deleteOpen} onClose={() => setDeleteOpen(false)} title="حذف سؤال؟" description="این سؤال از پیش‌نویس آزمون حذف می‌شود. این عمل در نمونهٔ فعلی قابل بازگردانی نیست." size="sm"><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><Button variant="outline" onClick={() => setDeleteOpen(false)}>انصراف</Button><Button variant="destructive" data-autofocus onClick={deleteSelected}><Trash2 className="h-4 w-4"/>حذف سؤال</Button></div></Dialog></div>;
}
function QuestionTypePicker({ onPick }: { onPick: (type: QuestionType) => void }) { return <div className="rounded-2xl border border-primary/20 bg-primary/[.04] p-3"><p className="mb-2 text-[11px] font-black text-primary">انتخاب نوع سؤال</p><div className="grid gap-1">{questionTypes.map((item) => <button type="button" key={item.type} onClick={() => onPick(item.type)} className="rounded-xl px-2 py-2 text-right hover:bg-card"><span className="text-xs font-bold">{item.label}</span><span className="mr-1 text-[10px] text-muted-foreground">{item.description}</span></button>)}</div></div>; }
function QuestionEmpty({ onAdd }: { onAdd: () => void }) { return <Card className="grid min-h-80 place-items-center border-dashed bg-muted/20"><div className="p-6 text-center"><ListPlus className="mx-auto h-8 w-8 text-primary"/><h3 className="mt-4 font-black">هنوز سؤالی انتخاب نشده</h3><p className="mt-2 text-sm text-muted-foreground">یک سؤال جدید بسازید تا ویرایش آن را شروع کنید.</p><Button className="mt-5" onClick={onAdd}>افزودن سؤال</Button></div></Card>; }
function QuestionEditor({ question, onChange, onPreview, onDuplicate, onDelete, onMove }: { question: Question; onChange: (question: Question) => void; onPreview: () => void; onDuplicate: () => void; onDelete: () => void; onMove: (direction: -1 | 1) => void }) { function changeType(type: QuestionType) { if (type === question.type) return; const fresh = blankQuestion(type, question.order); onChange({ ...fresh, id: question.id, stem: question.stem, points: question.points, helpText: question.helpText, explanation: question.explanation }); }
  return <Card className="overflow-hidden"><CardHeader className="border-b"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="section-label">ویرایش سؤال {toPersianNumber(question.order)}</p><CardTitle className="mt-1">{questionTypeLabel[question.type]}</CardTitle></div><div className="flex gap-1"><Button type="button" variant="ghost" size="icon-sm" onClick={() => onMove(-1)} aria-label="انتقال سؤال به بالا"><ChevronUp className="h-4 w-4"/></Button><Button type="button" variant="ghost" size="icon-sm" onClick={() => onMove(1)} aria-label="انتقال سؤال به پایین"><ChevronDown className="h-4 w-4"/></Button><Button type="button" variant="ghost" size="icon-sm" onClick={onPreview} aria-label="پیش‌نمایش سؤال"><Eye className="h-4 w-4"/></Button><Button type="button" variant="ghost" size="icon-sm" onClick={onDuplicate} aria-label="تکثیر سؤال"><Copy className="h-4 w-4"/></Button><Button type="button" variant="ghost" size="icon-sm" className="text-destructive" onClick={onDelete} aria-label="حذف سؤال"><Trash2 className="h-4 w-4"/></Button></div></div></CardHeader><CardContent className="p-5 sm:p-6"><div className="grid gap-4 sm:grid-cols-[1fr_130px]"><label className="block text-sm font-bold">نوع سؤال<select value={question.type} onChange={(event) => changeType(event.target.value as QuestionType)} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 text-sm">{questionTypes.map((item) => <option key={item.type} value={item.type}>{item.label}</option>)}</select></label><label className="block text-sm font-bold">نمره<Input className="mt-2" type="number" min="0" max="100" value={question.points} onChange={(event) => onChange({ ...question, points: Math.max(0, Number(event.target.value)) })}/></label></div><label className="mt-5 block text-sm font-bold">متن سؤال<Textarea className="mt-2 min-h-28" value={question.stem} onChange={(event) => onChange({ ...question, stem: event.target.value })} placeholder="متن روشن و کامل سؤال را وارد کنید..." maxLength={1000}/><span className="mt-1 block text-left text-[10px] font-bold text-muted-foreground">{toPersianNumber(question.stem.length)} / ۱۰۰۰</span></label>{question.type === "single_choice" && <SingleChoiceEditor question={question} onChange={onChange}/>} {question.type === "multiple_choice" && <MultipleAnswerEditor question={question} onChange={onChange}/>} {question.type === "true_false" && <TrueFalseEditor question={question} onChange={onChange}/>} {question.type === "short_answer" && <ShortAnswerEditor question={question} onChange={onChange}/>} {question.type === "essay" && <WrittenEditor question={question} onChange={onChange}/>}<label className="mt-5 block text-sm font-bold">توضیح یا بازخورد (اختیاری)<Textarea className="mt-2 min-h-20" value={question.explanation ?? ""} onChange={(event) => onChange({ ...question, explanation: event.target.value })} placeholder="توضیحی که بعداً می‌تواند به دانش‌آموز نمایش داده شود..."/></label><div className="mt-5 flex items-center justify-between border-t pt-5"><span className="flex items-center gap-1.5 text-xs font-bold text-muted-foreground">{questionIsComplete(question) ? <><CircleCheck className="h-4 w-4 text-emerald-600"/>آماده برای انتشار</> : <><span className="h-2 w-2 rounded-full bg-amber-500"/>نیازمند تکمیل</>}</span><Badge variant="neutral">به‌صورت خودکار ذخیره می‌شود</Badge></div></CardContent></Card>; }
function OptionsEditor({ options, onChange, correctIds, multiple }: { options: QuestionOption[]; onChange: (options: QuestionOption[], correctIds: string[]) => void; correctIds: string[]; multiple: boolean }) { function updateLabel(index: number, label: string) { onChange(options.map((option, optionIndex) => optionIndex === index ? { ...option, label } : option), correctIds); } function setCorrect(id: string) { const nextIds = multiple ? (correctIds.includes(id) ? correctIds.filter((item) => item !== id) : [...correctIds, id]) : [id]; onChange(options.map((option) => ({ ...option, isCorrect: nextIds.includes(option.id) })), nextIds); } function addOption() { const id = `option-${Date.now().toString(36)}`; onChange([...options, { id, label: "", value: id }], correctIds); } function deleteOption(id: string) { const next = options.filter((option) => option.id !== id); onChange(next, correctIds.filter((item) => item !== id)); }
 return <section className="mt-5"><div className="flex items-center justify-between"><div><h3 className="text-sm font-black">گزینه‌ها</h3><p className="mt-1 text-xs text-muted-foreground">{multiple ? "یک یا چند پاسخ درست را علامت بزنید." : "یک پاسخ درست را انتخاب کنید."}</p></div><Button type="button" variant="outline" size="sm" onClick={addOption}><Plus className="h-3.5 w-3.5"/>گزینه</Button></div><div className="mt-3 space-y-2">{options.map((option, index) => { const checked = correctIds.includes(option.id); return <div className={cn("flex items-center gap-2 rounded-xl border p-2 transition-colors", checked && "border-emerald-500/35 bg-emerald-500/[.04]")} key={option.id}><button type="button" role={multiple ? "checkbox" : "radio"} aria-checked={checked} onClick={() => setCorrect(option.id)} className={cn("grid h-8 w-8 shrink-0 place-items-center rounded-lg border text-xs", checked ? "border-emerald-600 bg-emerald-600 text-white" : "bg-card text-muted-foreground")} aria-label={`علامت‌گذاری گزینه ${index + 1} به‌عنوان پاسخ درست`}>{checked ? <Check className="h-4 w-4"/> : toPersianNumber(index + 1)}</button><Input value={option.label} onChange={(event) => updateLabel(index, event.target.value)} placeholder={`گزینهٔ ${toPersianNumber(index + 1)}`}/><button type="button" onClick={() => deleteOption(option.id)} className="rounded-lg p-2 text-muted-foreground hover:bg-rose-500/10 hover:text-destructive" aria-label="حذف گزینه" disabled={options.length <= 2}><Trash2 className="h-4 w-4"/></button></div>; })}</div></section>; }
function SingleChoiceEditor({ question, onChange }: { question: Extract<Question, { type: "single_choice" }>; onChange: (question: Question) => void }) { return <OptionsEditor options={question.options} correctIds={question.correctOptionId ? [question.correctOptionId] : []} multiple={false} onChange={(options, ids) => onChange({ ...question, options, correctOptionId: ids[0] ?? "" })}/>; }
function MultipleAnswerEditor({ question, onChange }: { question: Extract<Question, { type: "multiple_choice" }>; onChange: (question: Question) => void }) { return <OptionsEditor options={question.options} correctIds={question.correctOptionIds ?? []} multiple onChange={(options, ids) => onChange({ ...question, options, correctOptionIds: ids })}/>; }
function TrueFalseEditor({ question, onChange }: { question: Extract<Question, { type: "true_false" }>; onChange: (question: Question) => void }) { return <section className="mt-5"><h3 className="text-sm font-black">پاسخ درست</h3><div className="mt-3 grid grid-cols-2 gap-3">{[{ label: "درست", value: true }, { label: "نادرست", value: false }].map((item) => <button key={item.label} type="button" onClick={() => onChange({ ...question, correctAnswer: item.value })} className={cn("rounded-xl border p-3 text-sm font-bold", question.correctAnswer === item.value ? "border-emerald-500 bg-emerald-500/10 text-emerald-700 dark:text-emerald-400" : "hover:bg-muted")}>{question.correctAnswer === item.value && <Check className="ml-1 inline h-4 w-4"/>}{item.label}</button>)}</div></section>; }
function ShortAnswerEditor({ question, onChange }: { question: Extract<Question, { type: "short_answer" }>; onChange: (question: Question) => void }) { return <div className="mt-5 grid gap-4 sm:grid-cols-[1fr_150px]"><label className="block text-sm font-bold">پاسخ مورد انتظار<Input className="mt-2" value={question.expectedAnswer ?? ""} onChange={(event) => onChange({ ...question, expectedAnswer: event.target.value })} placeholder="مثلاً اسید آمینه"/></label><label className="block text-sm font-bold">حداکثر کاراکتر<Input className="mt-2" type="number" min="1" value={question.maxLength ?? 150} onChange={(event) => onChange({ ...question, maxLength: Math.max(1, Number(event.target.value)) })}/></label></div>; }
function WrittenEditor({ question, onChange }: { question: Extract<Question, { type: "essay" }>; onChange: (question: Question) => void }) { return <div className="mt-5 grid gap-4"><label className="block text-sm font-bold">راهنمای پاسخ (اختیاری)<Input className="mt-2" value={question.helpText ?? ""} onChange={(event) => onChange({ ...question, helpText: event.target.value })} placeholder="مثلاً پاسخ را با یک مثال توضیح دهید."/></label><div className="grid gap-4 sm:grid-cols-[150px_1fr]"><label className="block text-sm font-bold">حداکثر کاراکتر<Input className="mt-2" type="number" min="50" value={question.maxLength ?? 1200} onChange={(event) => onChange({ ...question, maxLength: Math.max(50, Number(event.target.value)) })}/></label><label className="block text-sm font-bold">یادداشت برای تصحیح<Textarea className="mt-2 min-h-11" value={question.gradingNote ?? ""} onChange={(event) => onChange({ ...question, gradingNote: event.target.value })} placeholder="نکات مورد انتظار از پاسخ..."/></label></div></div>; }
