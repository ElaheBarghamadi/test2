"use client";
import { motion } from "framer-motion";
import { cn } from "@/lib/utils";
const values = [52, 66, 61, 82, 74, 88, 83];
export function PerformanceChart({ compact = false }: { compact?: boolean }) { return <div className={cn("flex h-48 items-end gap-2 pt-5", compact && "h-32")}>{values.map((value, index) => <div className="flex h-full flex-1 flex-col justify-end gap-2" key={index}><motion.div initial={{ height: 0 }} animate={{ height: `${value}%` }} transition={{ delay: index * .05, duration: .55, ease: "easeOut" }} className={cn("rounded-t-lg bg-primary/20", index === values.length - 1 && "bg-primary")} title={`${value}%`} /><span className="text-center text-[9px] font-bold text-muted-foreground">{["ش", "ی", "د", "س", "چ", "پ", "ج"][index]}</span></div>)}</div>; }
export function DonutChart({ value = 88 }: { value?: number }) { const radius = 42; const circumference = 2 * Math.PI * radius; return <div className="relative grid h-28 w-28 place-items-center"><svg className="h-28 w-28 -rotate-90" viewBox="0 0 100 100"><circle cx="50" cy="50" r={radius} fill="none" stroke="currentColor" strokeWidth="9" className="text-muted"/><circle cx="50" cy="50" r={radius} fill="none" stroke="currentColor" strokeWidth="9" strokeLinecap="round" strokeDasharray={circumference} strokeDashoffset={circumference * (1 - value / 100)} className="text-primary transition-all duration-700"/></svg><span className="absolute text-lg font-black">{value}٪</span></div>; }
