import type { LucideIcon } from "lucide-react";
import { TrendingDown, TrendingUp } from "lucide-react";
import { Card } from "@/components/ui/card";
import { cn } from "@/lib/utils";
export function StatCard({ label, value, detail, icon: Icon, trend, tone = "indigo" }: { label: string; value: string; detail: string; icon: LucideIcon; trend?: "up" | "down"; tone?: "indigo" | "teal" | "violet" | "amber" }) { const tones = { indigo: "bg-indigo-500/10 text-indigo-600", teal: "bg-teal-500/10 text-teal-600", violet: "bg-violet-500/10 text-violet-600", amber: "bg-amber-500/10 text-amber-600" }; const TrendIcon = trend === "down" ? TrendingDown : TrendingUp; return <Card className="p-5 transition-shadow hover:shadow-lift"><div className="flex items-start justify-between"><div className={cn("rounded-xl p-2.5", tones[tone])}><Icon className="h-5 w-5"/></div>{trend && <span className={cn("flex items-center gap-1 text-[11px] font-extrabold", trend === "up" ? "text-emerald-600" : "text-rose-600")}><TrendIcon className="h-3.5 w-3.5"/>{trend === "up" ? "رو به رشد" : "نیازمند توجه"}</span>}</div><p className="mt-5 text-2xl font-black tracking-tight">{value}</p><p className="mt-1 text-xs font-bold text-muted-foreground">{label}</p><p className="mt-3 text-[11px] text-muted-foreground">{detail}</p></Card>; }
